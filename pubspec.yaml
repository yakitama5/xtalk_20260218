name: xtalk_20260218
description: "XTalk 発表資料：その矢印アイコン、実装コストは100万円です"
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  flutter_deck: ^0.22.0
  flutter_deck_client: ^0.3.0
  flutter_deck_web_client: ^0.4.0
  gap: ^3.0.1
  google_fonts: ^8.0.1
  qr_flutter: ^4.1.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
